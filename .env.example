# Example environment variables for Fitness Academy (create .env.local from this)

# Database
# PostgreSQL connection - replace user/password/host/db as needed
DATABASE_URL="postgresql://user:password@localhost:5432/fitdb?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace_with_a_secure_random_value"

# Application
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Stripe
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Cloudinary (optional)
# Format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_URL=cloudinary://your_api_key:your_api_secret@your_cloud_name
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name

# Email / SMTP (used by mailer in lib/mailer.ts)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
EMAIL_FROM="Fitness Academy <no-reply@example.com>"

# Other provider secrets (OAuth, etc.)
# GITHUB_ID=...
# GITHUB_SECRET=...

# Notes:
# - Never commit real secrets. Use a separate secrets manager or CI environment variables for production.
# - After updating .env.local, restart the Next.js server to pick up changes.
